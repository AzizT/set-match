{% extends 'base.html.twig' %}

{% block title %}Planning{% endblock %}

{% block content %}
    <h1>Planning</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planning.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ planning.dateDebut ? planning.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ planning.dateFin ? planning.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('planning_index') }}">back to list</a> #}

    {# <a href="{{ path('planning_edit', {'id': planning.id}) }}">edit</a> #}

    {# {{ include('planning/_delete_form.html.twig') }} #}
{% endblock %}
